ignore_errors: true
--------------------------------------------------------
is defined
default plugin especially default(omit) to avoid setting something if it is not declared/defined or default('whatever text') in debug messages
Block, always, rescue (but can't loop through entire block)
notify: "handler_task_name"
when: "registered_variable".changed
template lookup plugin

Block is a logical group for tasks
group and use conditionals on a set of tasks
block: define the main set of tasks to run
rescue: to define tasks to run when the main tasks within block fails
always: tasks that will always run
--------------------------------------------------------------------------------

ignore_errors
force_handlers
failed_when:
Alternatively, fail module can be used to print a message why a task has failed
result must be registered and the result analysed
---------------------------------------------------------------

tags to mark a task or a block with metadata
limit with --tags or --skip-tags

----------------------------------------------------------------

check mode --check (gather info tasks will not run). Can disable using `check_mode: false` locally
--------------------------------------------------------------------------------

assert module, that, fail_msg, success_msg, ansible_memtotal_mb
like fail module, but also has a success message
--------------------------------------------------------------------------------
stat module to test a file system path presence
registered_variable.state.isdir for example
--------------------------------------------------------------------------------
failed_when:
changed_when: false (useful for command and shell tasks)
--------------------------------------------------------------------------------
nested loops achieved by using `include_tasks` and specifying those tasks yamls. This will execute as the inner loop
loop_control:
  loop_var: whatever (to replace `item`), to change the external loop variable to something else
-------------------------------------------------------------------------------

`groups` is a magic variable

--------------------------------------------------------------------------------
Magic Variables
---------------
hostvars
groupvars
inventory_hostname
inventory_hostname_short
groups
group_names
ansible_check_mode
ansible_play_batch/ansible_play_hosts
ansible_version

--------------------------------------------------------------------------------

loop:
notify keyword, handler tasks (are run at the end of a play only)
meta: flush_handlers or force_handlers: true
will run only if the task changes something

meta module to change the execution order
meta: flush_handlers, refresh_inventory, clear_facts, end_hosts

when:
==, <, >, <=, >=, !=, is defined, is not defined, in
data type: string, numbers, bool, dates
and, or, not.

when can use a list which is equivalent to an `and` condition

loop: "{{ ansible_mounts }}"
when: item.mount == "/boot" and item.size_available > 10000000
--------------------------------------------------------------------------------

vars_prompt:
- name:
  prompt:
  private:



when: registered_variable.stdout.find(someothervarname) != -1
--------------------------------------------------------------------------------
ansible.posix.synchronize (delegate_to)
copy, fetch, stat. fetch with flat is useful for fetching from one host
copy module does not support wildcards, but can combine with the fileglob plugin to achieve this
--------------------------------------------------------------------------------
|  keeps formatting
>  does not keep formatting
replace module with regexp arguments
lineinfile
blockinfile
/etc/ansible/facts.d/local.fact can contain custom facts
if you prefer a different directory, create it and specify it using the fact_path play keyword.
[localfact]
key: value  e.g. type: production
To add static facts, simply add a file with the .fact extension. For example, create /etc/ansible/facts.d/preferences.fact with this content:

[general]
asdf=1
bar=2
--------------------------------------------------------------------------------
find module (like linux find command)
--------------------------------------------------------------------------------
template module is like copy module except it processes jinja2 templates beforehand

--------------------------------------------------------------------------------
variables can be piped into filters
Common filters
--------------
+ - * /
capitalise
default {{ my_var | default(myvalue, True) }}
dict2items
flatten
float
join (helps to get the output on a single line)
int
list
mandatory
map (e.g. with 'relpath' argument, or 'combine' argument)
password_hash
quote
random
selectattr
sort
lookup(subelements)
union

ipaddr (from the ansible.utils collection)
--------------------------------------------------------------------------------
set_facts to change one variable type through another by using a filter and assigning it to a new fact

Type of plugins
---------------
action (sort of a default plugin)
cache
callback
connection
filter
inventory
lookup  (lookup and query). lookup('file'), lookup('template'), lookup('env'), lookup('url'), query('pipe'), lookup('k8s'), query('file'), lookup('random'), lookup('lines'). 'pipe': unformatted (raw) output of a command, 'lines': content of a file,
query('fileglob'), lookup('fileglob')
test
password lookup plugin. lookup('password', ..., ...). After creating the password and saving it to a file, can pipe it to the password_hash filter to create a user with the hashed password
--------------------------------------------------------------------------------

fact_caching with redis plugin
sudo dnf install -y redis
sudo systemctl enable --now redis
pip install redis
ansible-galaxy collection install community.general

ansible.cfg
-----------
fact_caching = redis
fact_caching_timeout = 3600
fact_caching_connection = localhost:6379:0
run a scheduled job to refresh the facts cache periodically
ansible-playbook --flush-cache to avoid caching and start fact_gathering

